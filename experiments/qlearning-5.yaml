# Experimento 1 - Factor de descuento & tasa de aprendizaje
algorithm: qlearning

parameters:
  gamma: # Take best values from previous experiments
  learning_rate: # Take best values from previous experiments
  epsilon: # Take best values from previous experiments
  num_episodes: # Take best values from previous experiments
  eval_episodes: 500
  num_runs: 20
  ep_decay: # Take best values from previous experiments
  lr_decay: # Take best values from previous experiments
  penalty: [-1.0, -2.0, -10.0, -50.0]
